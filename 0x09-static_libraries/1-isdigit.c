#include "main.h"

/**
 * _isdigit - function to check whether a given character is a decimal digit
 * @c: int to be reviewed
 * Return: 1 or 0 depending on outcome
 */

int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
